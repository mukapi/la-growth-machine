{
  "name": "la-growth-machine",
  "version": "1.0.0",
  "description": "La Growth Machine website scripts",
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development node ./bin/build.js",
    "build": "cross-env NODE_ENV=production node ./bin/build.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "esbuild": "^0.24.2"
  }
}
